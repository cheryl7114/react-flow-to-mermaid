/**
 * Copyright IBM Corp. 2019, 2025
 *
 * This source code is licensed under the Apache-2.0 license found in the
 * LICENSE file in the root directory of this source tree.
 */
import { TAG_SIZE, TAG_TYPE } from './defs';
import CDSTag from '../tag/tag';
import '../tooltip/index';
export { TAG_SIZE, TAG_TYPE };
declare const CDSDismissibleTag_base: {
    new (...args: any[]): {
        _handles: Set<import("../../globals/internal/handle").default>;
        connectedCallback(): void;
        disconnectedCallback(): void;
    };
    _hostListeners: {
        [listenerName: string]: {
            [type: string]: {
                options?: boolean | AddEventListenerOptions;
            };
        };
    };
} & (new (...args: any[]) => {
    focus(): void;
}) & typeof CDSTag;
/**
 * Dismissible Tag.
 *
 * @element cds-dismissible-tag
 *
 * @fires cds-dismissible-tag-beingclosed - The custom event fired as the element is being closed
 * @fires cds-dismissible-tag-closed - The custom event fired after the element has been closed
 */
declare class CDSDismissibleTag extends CDSDismissibleTag_base {
    protected _buttonNode: HTMLButtonElement;
    /**
     * Finds the next focusable dismissible tag sibling
     * @returns {HTMLElement|null} The next focusable dismissible tag or null
     */
    protected _findNextFocusableTag(): Element | null;
    /**
     * Handles `slotchange` event.
     */
    protected _handleAILabelSlotChange({ target }: Event): void;
    /**
     * Handles `click` event on this element.
     *
     * @param event The event.
     */
    protected _handleClick: (event: MouseEvent) => void;
    /**
     * `true` if the tag should be disabled
     */
    disabled: boolean;
    /**
     * Specify the tooltip alignment for the dismiss button
     */
    dismissTooltipAlignment: string;
    /**
     * Provide a custom tooltip label for the dismiss button
     */
    dismissTooltipLabel?: string;
    /**
     * `true` if the tag should be open.
     */
    open: boolean;
    /**
     * The size of the tag.
     */
    size: TAG_SIZE;
    /**
     * Provide a custom `title` to be inserted in the tag.
     */
    tagTitle: string;
    /**
     * Provide text to be rendered inside of a the tag.
     */
    text: string;
    /**
     * The type of the tag.
     */
    type: TAG_TYPE;
    render(): import("lit-html").TemplateResult<1>;
    /**
     * A selector that will return the slug item.
     *
     * remove in v12
     */
    static get slugItem(): string;
    /**
     * A selector that will return the AI Label item.
     */
    static get aiLabelItem(): string;
    /**
     * The name of the custom event fired before this tag is being closed upon a user gesture.
     * Cancellation of this event stops the user-initiated action of closing this tag.
     */
    static get eventBeforeClose(): string;
    /**
     * The name of the custom event fired after this tag is closed upon a user gesture.
     */
    static get eventClose(): string;
    static styles: any;
}
export default CDSDismissibleTag;
//# sourceMappingURL=dismissible-tag.d.ts.map