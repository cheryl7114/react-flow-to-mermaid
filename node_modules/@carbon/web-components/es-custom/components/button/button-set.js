/**
 * Copyright IBM Corp. 2026
 *
 * This source code is licensed under the Apache-2.0 license found in the
 * LICENSE file in the root directory of this source tree.
 */

import { prefix } from "../../globals/settings.js";
import { carbonElement } from "../../globals/decorators/carbon-element.js";
import { __decorate } from "../../_virtual/_@oxc-project_runtime@0.112.0/helpers/decorate.js";
import { BUTTON_KIND } from "./defs.js";
import button_default from "./button.scss.js";
import button_set_base_default from "./button-set-base.js";
import { html } from "lit";
import { property } from "lit/decorators.js";
import { classMap } from "lit/directives/class-map.js";

//#region src/components/button/button-set.ts
/**
* Copyright IBM Corp. 2020, 2024
*
* This source code is licensed under the Apache-2.0 license found in the
* LICENSE file in the root directory of this source tree.
*/
/**
* Button set.
*
* @element cds-custom-button-set
*/
let CDSButtonSet = class CDSButtonSet extends button_set_base_default {
	constructor(..._args) {
		super(..._args);
		this.stacked = false;
		this._hideSiblingMargin = () => {
			const slot = this.shadowRoot?.querySelector("slot");
			if (!slot) return;
			const items = slot.assignedElements().filter((el) => el.tagName.toLowerCase() === `${prefix}-button`);
			const focusedIndex = items.findIndex((el) => el.matches(":focus-within"));
			items.forEach((el, idx) => {
				const shouldHide = focusedIndex >= 0 && (idx === focusedIndex || idx === focusedIndex + 1);
				el.toggleAttribute("hide-margin", shouldHide);
			});
		};
	}
	/**
	* Handler for @slotchange, set the first cds-custom-button to kind secondary and primary for the remaining ones
	*
	* @private
	*/
	_handleSlotChange(event) {
		event.target.assignedNodes().filter((elem) => elem.matches !== void 0 ? elem.matches(this.constructor.selectorItem) : false).forEach((elem, index) => {
			elem.setAttribute("kind", index === 0 ? BUTTON_KIND.SECONDARY : BUTTON_KIND.PRIMARY);
		});
		const update = new CustomEvent(`${prefix}-btn-set-update`, {
			bubbles: true,
			cancelable: true,
			composed: true
		});
		this.dispatchEvent(update);
	}
	connectedCallback() {
		super.connectedCallback?.();
		this.addEventListener("focusin", this._hideSiblingMargin);
		this.addEventListener("focusout", this._hideSiblingMargin);
	}
	render() {
		const { stacked } = this;
		return html`<slot
      class="${classMap({
			[`${prefix}--btn-set--stacked`]: stacked,
			[`${prefix}--btn-set`]: true
		})}"
      part="button-set"
      @slotchange="${this._handleSlotChange}"></slot>`;
	}
	/**
	* A selector that will return the child items.
	*/
	static get selectorItem() {
		return `${prefix}-button`;
	}
	static {
		this.styles = button_default;
	}
};
__decorate([property({
	type: Boolean,
	reflect: true
})], CDSButtonSet.prototype, "stacked", void 0);
CDSButtonSet = __decorate([carbonElement(`${prefix}-button-set`)], CDSButtonSet);
var button_set_default = CDSButtonSet;

//#endregion
export { button_set_default as default };
//# sourceMappingURL=button-set.js.map